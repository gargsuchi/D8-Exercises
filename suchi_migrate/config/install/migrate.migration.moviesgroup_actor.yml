# Migration configuration for actor content.
id: moviesgroup_actor
label: Actors
third_party_settings:
  migrate_plus:
    migration_group: moviesgroup
source:
  plugin: moviesgroup_actor
destination:
  plugin: entity:node
process:
  # Hardcode the destination node type (bundle) as 'migrate_example_beer'.
  type:
    plugin: default_value
    default_value: actors
  title: name
